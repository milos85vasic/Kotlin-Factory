versionPackage = "net.milosvasic.factory"
packageStructure = "net${File.separator}milosvasic${File.separator}factory"

repositories {
    jcenter()
    mavenCentral()
}

dependencies {
    compile files("libs/Logger_V1.1.2.jar")
}
